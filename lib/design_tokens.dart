// ── Serene Design Tokens ───────────────────────────────────────────────────────
// Shared across all screens. Import this file instead of redeclaring constants.

import 'package:flutter/material.dart';

// Background
const kBg         = Color(0xFFEDEAE6); // warm soft gray (was #FAF9F7)
const kCardBg     = Color(0xFFE8E4F0); // warmer lavender card bg (was white)
const kCardWhite  = Colors.white;      // pure white when needed for contrast

// Accent
const kAccent     = Color(0xFF7C6FA0); // muted purple (unchanged)
const kAccentMild = Color(0xFFEDE9F5); // light lavender tint (unchanged)

// Player card
const kPlayerBg   = Color(0xFF6B6090); // softened dark purple for audio card (was #7C6FA0)

// Text
const kTextMain   = Color(0xFF1A1A2E);
const kTextSub    = Color(0xFF8A8A9A);

// Divider
const kDivider    = Color(0xFFE0DCF0); // slightly purple-tinted divider

// Terracotta (logout)
const kTerracotta = Color(0xFFC4735A);

// Favourite heart — muted rose (was pinkAccent / Colors.pinkAccent)
const kHeartColor = Color(0xFFC4829A);

// Border radius
const kRadius = 16.0;